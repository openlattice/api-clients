/**
 * OpenLattice API
 * OpenLattice API
 *
 * The version of the OpenAPI document: 0.0.1
 * Contact: support@openlattice.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FlightPlanParameters from './FlightPlanParameters';

/**
 * The Integration model module.
 * @module model/Integration
 * @version 0.0.1
 */
class Integration {
    /**
     * Constructs a new <code>Integration</code>.
     * Represents a data integration, including all fields required to run the integration.
     * @alias module:model/Integration
     */
    constructor() { 
        
        Integration.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Integration</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Integration} obj Optional instance to populate.
     * @return {module:model/Integration} The populated <code>Integration</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Integration();

            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('environment')) {
                obj['environment'] = ApiClient.convertToType(data['environment'], 'String');
            }
            if (data.hasOwnProperty('s3Bucket')) {
                obj['s3Bucket'] = ApiClient.convertToType(data['s3Bucket'], 'String');
            }
            if (data.hasOwnProperty('contacts')) {
                obj['contacts'] = ApiClient.convertToType(data['contacts'], ['String']);
            }
            if (data.hasOwnProperty('organizationId')) {
                obj['organizationId'] = ApiClient.convertToType(data['organizationId'], 'String');
            }
            if (data.hasOwnProperty('entitySetId')) {
                obj['entitySetId'] = ApiClient.convertToType(data['entitySetId'], 'String');
            }
            if (data.hasOwnProperty('maxConnections')) {
                obj['maxConnections'] = ApiClient.convertToType(data['maxConnections'], 'Number');
            }
            if (data.hasOwnProperty('callback')) {
                obj['callback'] = ApiClient.convertToType(data['callback'], ['String']);
            }
            if (data.hasOwnProperty('flightPlanParameters')) {
                obj['flightPlanParameters'] = ApiClient.convertToType(data['flightPlanParameters'], {'String': FlightPlanParameters});
            }
        }
        return obj;
    }


}

/**
 * a unique ID used for authorizing a call to run an integration
 * @member {String} key
 */
Integration.prototype['key'] = undefined;

/**
 * the retrofit environment (e.g. PROD_INTEGRATION, LOCAL)
 * @member {module:model/Integration.EnvironmentEnum} environment
 */
Integration.prototype['environment'] = undefined;

/**
 * the url of the s3bucket to be used
 * @member {String} s3Bucket
 */
Integration.prototype['s3Bucket'] = undefined;

/**
 * the set of email addresses of those responsible for the integration
 * @member {Array.<String>} contacts
 */
Integration.prototype['contacts'] = undefined;

/**
 * the id of the organization that owns the integration
 * @member {String} organizationId
 */
Integration.prototype['organizationId'] = undefined;

/**
 * the id of the entity set that stores the logs for this integration
 * @member {String} entitySetId
 */
Integration.prototype['entitySetId'] = undefined;

/**
 * maximum number of connections to postgres allowed for this integration
 * @member {Number} maxConnections
 */
Integration.prototype['maxConnections'] = undefined;

/**
 * urls to receive a POST when integration has completed
 * @member {Array.<String>} callback
 */
Integration.prototype['callback'] = undefined;

/**
 * a map from [Flight] name to [FlightPlanParameters]
 * @member {Object.<String, module:model/FlightPlanParameters>} flightPlanParameters
 */
Integration.prototype['flightPlanParameters'] = undefined;





/**
 * Allowed values for the <code>environment</code> property.
 * @enum {String}
 * @readonly
 */
Integration['EnvironmentEnum'] = {

    /**
     * value: "PROD_INTEGRATION"
     * @const
     */
    "PROD_INTEGRATION": "PROD_INTEGRATION",

    /**
     * value: "STAGING_INTEGRATION"
     * @const
     */
    "STAGING_INTEGRATION": "STAGING_INTEGRATION",

    /**
     * value: "LOCAL"
     * @const
     */
    "LOCAL": "LOCAL"
};



export default Integration;

