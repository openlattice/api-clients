# OpenLattice API
#
# OpenLattice API
#
# The version of the OpenAPI document: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FlightPlanParameters
#'
#' @description FlightPlanParameters Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field sql  character [optional]
#'
#' @field src  named list( character ) [optional]
#'
#' @field srcPrimaryKeyColumns  list( character ) [optional]
#'
#' @field path  character [optional]
#'
#' @field flight  \link{Flight} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FlightPlanParameters <- R6::R6Class(
  'FlightPlanParameters',
  public = list(
    `sql` = NULL,
    `src` = NULL,
    `srcPrimaryKeyColumns` = NULL,
    `path` = NULL,
    `flight` = NULL,
    initialize = function(
        `sql`=NULL, `src`=NULL, `srcPrimaryKeyColumns`=NULL, `path`=NULL, `flight`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`sql`)) {
        stopifnot(is.character(`sql`), length(`sql`) == 1)
        self$`sql` <- `sql`
      }
      if (!is.null(`src`)) {
        stopifnot(is.vector(`src`))
        sapply(`src`, function(x) stopifnot(is.character(x)))
        self$`src` <- `src`
      }
      if (!is.null(`srcPrimaryKeyColumns`)) {
        stopifnot(is.vector(`srcPrimaryKeyColumns`))
        sapply(`srcPrimaryKeyColumns`, function(x) stopifnot(is.character(x)))
        self$`srcPrimaryKeyColumns` <- `srcPrimaryKeyColumns`
      }
      if (!is.null(`path`)) {
        stopifnot(is.character(`path`), length(`path`) == 1)
        self$`path` <- `path`
      }
      if (!is.null(`flight`)) {
        stopifnot(R6::is.R6(`flight`))
        self$`flight` <- `flight`
      }
    },
    toJSON = function() {
      FlightPlanParametersObject <- list()
      if (!is.null(self$`sql`)) {
        FlightPlanParametersObject[['sql']] <-
          self$`sql`
      }
      if (!is.null(self$`src`)) {
        FlightPlanParametersObject[['src']] <-
          self$`src`
      }
      if (!is.null(self$`srcPrimaryKeyColumns`)) {
        FlightPlanParametersObject[['srcPrimaryKeyColumns']] <-
          self$`srcPrimaryKeyColumns`
      }
      if (!is.null(self$`path`)) {
        FlightPlanParametersObject[['path']] <-
          self$`path`
      }
      if (!is.null(self$`flight`)) {
        FlightPlanParametersObject[['flight']] <-
          self$`flight`$toJSON()
      }

      FlightPlanParametersObject
    },
    fromJSON = function(FlightPlanParametersJson) {
      FlightPlanParametersObject <- jsonlite::fromJSON(FlightPlanParametersJson)
      if (!is.null(FlightPlanParametersObject$`sql`)) {
        self$`sql` <- FlightPlanParametersObject$`sql`
      }
      if (!is.null(FlightPlanParametersObject$`src`)) {
        self$`src` <- ApiClient$new()$deserializeObj(FlightPlanParametersObject$`src`, "list(character)", loadNamespace("openlattice"))
      }
      if (!is.null(FlightPlanParametersObject$`srcPrimaryKeyColumns`)) {
        self$`srcPrimaryKeyColumns` <- ApiClient$new()$deserializeObj(FlightPlanParametersObject$`srcPrimaryKeyColumns`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(FlightPlanParametersObject$`path`)) {
        self$`path` <- FlightPlanParametersObject$`path`
      }
      if (!is.null(FlightPlanParametersObject$`flight`)) {
        flightObject <- Flight$new()
        flightObject$fromJSON(jsonlite::toJSON(FlightPlanParametersObject$flight, auto_unbox = TRUE, digits = NA))
        self$`flight` <- flightObject
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`sql`)) {
        sprintf(
        '"sql":
          "%s"
                ',
        self$`sql`
        )},
        if (!is.null(self$`src`)) {
        sprintf(
        '"src":
          %s
        ',
        jsonlite::toJSON(lapply(self$`src`, function(x){ x }), auto_unbox = TRUE, digits=NA)
        )},
        if (!is.null(self$`srcPrimaryKeyColumns`)) {
        sprintf(
        '"srcPrimaryKeyColumns":
           [%s]
        ',
        paste(unlist(lapply(self$`srcPrimaryKeyColumns`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`path`)) {
        sprintf(
        '"path":
          "%s"
                ',
        self$`path`
        )},
        if (!is.null(self$`flight`)) {
        sprintf(
        '"flight":
        %s
        ',
        jsonlite::toJSON(self$`flight`$toJSON(), auto_unbox=TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(FlightPlanParametersJson) {
      FlightPlanParametersObject <- jsonlite::fromJSON(FlightPlanParametersJson)
      self$`sql` <- FlightPlanParametersObject$`sql`
      self$`src` <- ApiClient$new()$deserializeObj(FlightPlanParametersObject$`src`, "list(character)", loadNamespace("openlattice"))
      self$`srcPrimaryKeyColumns` <- ApiClient$new()$deserializeObj(FlightPlanParametersObject$`srcPrimaryKeyColumns`, "array[character]", loadNamespace("openlattice"))
      self$`path` <- FlightPlanParametersObject$`path`
      self$`flight` <- Flight$new()$fromJSON(jsonlite::toJSON(FlightPlanParametersObject$flight, auto_unbox = TRUE, digits = NA))
      self
    }
  )
)

