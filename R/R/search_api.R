# OpenLattice API
#
# OpenLattice API
#
# OpenAPI spec version: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @title Search operations
#' @description openlattice.Search
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' ExecuteEntityNeighborSearch Executes a search for all neighbors of an entity that are connected by an association
#'
#'
#' ExecuteFilteredEntityNeighborSearch Executes a search for all neighbors of multiple entities of the same entity set that are connected by an association
#'
#' }
#'
#' @importFrom caTools base64encode
#' @export
SearchApi <- R6::R6Class(
  'SearchApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    ExecuteEntityNeighborSearch = function(entity.set.id, entity.key.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      if (missing(`entity.key.id`)) {
        stop("Missing required parameter `entity.key.id`.")
      }

      urlPath <- "/datastore/search/{entitySetId}/{entityKeyId}"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      if (!missing(`entity.key.id`)) {
        urlPath <- gsub(paste0("\\{", "entityKeyId", "\\}"), `entity.key.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        NeighborEntityDetails$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ExecuteFilteredEntityNeighborSearch = function(entity.set.id, neighbor.search.filter, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      if (missing(`neighbor.search.filter`)) {
        stop("Missing required parameter `neighbor.search.filter`.")
      }

      if (!missing(`neighbor.search.filter`)) {
        body <- `neighbor.search.filter`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/search/{entitySetId}/neighbors/advanced"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        NeighborEntityDetails$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    }
  )
)
