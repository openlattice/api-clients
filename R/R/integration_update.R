# OpenLattice API
#
# OpenLattice API
#
# The version of the OpenAPI document: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title IntegrationUpdate
#'
#' @description IntegrationUpdate Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field environment  character [optional]
#'
#' @field s3Bucket  character [optional]
#'
#' @field contacts  list( character ) [optional]
#'
#' @field organizationId  character [optional]
#'
#' @field maxConnections  integer [optional]
#'
#' @field callbackUrls  list( character ) [optional]
#'
#' @field flightPlanParameters  named list( \link{FlightPlanParametersUpdate} ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
IntegrationUpdate <- R6::R6Class(
  'IntegrationUpdate',
  public = list(
    `environment` = NULL,
    `s3Bucket` = NULL,
    `contacts` = NULL,
    `organizationId` = NULL,
    `maxConnections` = NULL,
    `callbackUrls` = NULL,
    `flightPlanParameters` = NULL,
    initialize = function(
        `environment`=NULL, `s3Bucket`=NULL, `contacts`=NULL, `organizationId`=NULL, `maxConnections`=NULL, `callbackUrls`=NULL, `flightPlanParameters`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`environment`)) {
        stopifnot(is.character(`environment`), length(`environment`) == 1)
        self$`environment` <- `environment`
      }
      if (!is.null(`s3Bucket`)) {
        stopifnot(is.character(`s3Bucket`), length(`s3Bucket`) == 1)
        self$`s3Bucket` <- `s3Bucket`
      }
      if (!is.null(`contacts`)) {
        stopifnot(is.vector(`contacts`))
        sapply(`contacts`, function(x) stopifnot(is.character(x)))
        self$`contacts` <- `contacts`
      }
      if (!is.null(`organizationId`)) {
        stopifnot(is.character(`organizationId`), length(`organizationId`) == 1)
        self$`organizationId` <- `organizationId`
      }
      if (!is.null(`maxConnections`)) {
        stopifnot(is.numeric(`maxConnections`), length(`maxConnections`) == 1)
        self$`maxConnections` <- `maxConnections`
      }
      if (!is.null(`callbackUrls`)) {
        stopifnot(is.vector(`callbackUrls`))
        sapply(`callbackUrls`, function(x) stopifnot(is.character(x)))
        self$`callbackUrls` <- `callbackUrls`
      }
      if (!is.null(`flightPlanParameters`)) {
        stopifnot(is.vector(`flightPlanParameters`))
        sapply(`flightPlanParameters`, function(x) stopifnot(R6::is.R6(x)))
        self$`flightPlanParameters` <- `flightPlanParameters`
      }
    },
    toJSON = function() {
      IntegrationUpdateObject <- list()
      if (!is.null(self$`environment`)) {
        IntegrationUpdateObject[['environment']] <-
          self$`environment`
      }
      if (!is.null(self$`s3Bucket`)) {
        IntegrationUpdateObject[['s3Bucket']] <-
          self$`s3Bucket`
      }
      if (!is.null(self$`contacts`)) {
        IntegrationUpdateObject[['contacts']] <-
          self$`contacts`
      }
      if (!is.null(self$`organizationId`)) {
        IntegrationUpdateObject[['organizationId']] <-
          self$`organizationId`
      }
      if (!is.null(self$`maxConnections`)) {
        IntegrationUpdateObject[['maxConnections']] <-
          self$`maxConnections`
      }
      if (!is.null(self$`callbackUrls`)) {
        IntegrationUpdateObject[['callbackUrls']] <-
          self$`callbackUrls`
      }
      if (!is.null(self$`flightPlanParameters`)) {
        IntegrationUpdateObject[['flightPlanParameters']] <-
          lapply(self$`flightPlanParameters`, function(x) x$toJSON())
      }

      IntegrationUpdateObject
    },
    fromJSON = function(IntegrationUpdateJson) {
      IntegrationUpdateObject <- jsonlite::fromJSON(IntegrationUpdateJson)
      if (!is.null(IntegrationUpdateObject$`environment`)) {
        self$`environment` <- IntegrationUpdateObject$`environment`
      }
      if (!is.null(IntegrationUpdateObject$`s3Bucket`)) {
        self$`s3Bucket` <- IntegrationUpdateObject$`s3Bucket`
      }
      if (!is.null(IntegrationUpdateObject$`contacts`)) {
        self$`contacts` <- ApiClient$new()$deserializeObj(IntegrationUpdateObject$`contacts`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(IntegrationUpdateObject$`organizationId`)) {
        self$`organizationId` <- IntegrationUpdateObject$`organizationId`
      }
      if (!is.null(IntegrationUpdateObject$`maxConnections`)) {
        self$`maxConnections` <- IntegrationUpdateObject$`maxConnections`
      }
      if (!is.null(IntegrationUpdateObject$`callbackUrls`)) {
        self$`callbackUrls` <- ApiClient$new()$deserializeObj(IntegrationUpdateObject$`callbackUrls`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(IntegrationUpdateObject$`flightPlanParameters`)) {
        self$`flightPlanParameters` <- ApiClient$new()$deserializeObj(IntegrationUpdateObject$`flightPlanParameters`, "list(FlightPlanParametersUpdate)", loadNamespace("openlattice"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`environment`)) {
        sprintf(
        '"environment":
          "%s"
                ',
        self$`environment`
        )},
        if (!is.null(self$`s3Bucket`)) {
        sprintf(
        '"s3Bucket":
          "%s"
                ',
        self$`s3Bucket`
        )},
        if (!is.null(self$`contacts`)) {
        sprintf(
        '"contacts":
           [%s]
        ',
        paste(unlist(lapply(self$`contacts`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`organizationId`)) {
        sprintf(
        '"organizationId":
          "%s"
                ',
        self$`organizationId`
        )},
        if (!is.null(self$`maxConnections`)) {
        sprintf(
        '"maxConnections":
          %d
                ',
        self$`maxConnections`
        )},
        if (!is.null(self$`callbackUrls`)) {
        sprintf(
        '"callbackUrls":
           [%s]
        ',
        paste(unlist(lapply(self$`callbackUrls`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`flightPlanParameters`)) {
        sprintf(
        '"flightPlanParameters":
        %s
',
        jsonlite::toJSON(lapply(self$`flightPlanParameters`, function(x){ x$toJSON() }), auto_unbox = FALSE, digits=NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(IntegrationUpdateJson) {
      IntegrationUpdateObject <- jsonlite::fromJSON(IntegrationUpdateJson)
      self$`environment` <- IntegrationUpdateObject$`environment`
      self$`s3Bucket` <- IntegrationUpdateObject$`s3Bucket`
      self$`contacts` <- ApiClient$new()$deserializeObj(IntegrationUpdateObject$`contacts`, "array[character]", loadNamespace("openlattice"))
      self$`organizationId` <- IntegrationUpdateObject$`organizationId`
      self$`maxConnections` <- IntegrationUpdateObject$`maxConnections`
      self$`callbackUrls` <- ApiClient$new()$deserializeObj(IntegrationUpdateObject$`callbackUrls`, "array[character]", loadNamespace("openlattice"))
      self$`flightPlanParameters` <- ApiClient$new()$deserializeObj(IntegrationUpdateObject$`flightPlanParameters`, "list(FlightPlanParametersUpdate)", loadNamespace("openlattice"))
      self
    }
  )
)

