# OpenLattice API
#
# OpenLattice API
#
# OpenAPI spec version: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @title Data operations
#' @description openlattice.Data
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' ClearEntitySet Clears the data from a single entity set.
#'
#'
#' DeleteAllEntitiesFromEntitySet Clears the Entity matching the given Entity id and all of its neighbor Entities
#'
#'
#' GetEntitySetSize Gets the number of entities in an entity set.
#'
#'
#' LoadEntitySetData Gets an iterable containing the entity data, using property type FQNs as key
#'
#'
#' LoadFilteredEntitySetData Gets a list of entities by UUID&#39;s
#'
#' }
#'
#' @importFrom caTools base64encode
#' @export
DataApi <- R6::R6Class(
  'DataApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    ClearEntitySet = function(entity.set.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      urlPath <- "/datastore/data/set/{entitySetId}"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteAllEntitiesFromEntitySet = function(entity.set.id, type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }

      queryParams['type'] <- type

      urlPath <- "/datastore/data/set/{entitySetId}/all"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntitySetSize = function(entity.set.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      urlPath <- "/datastore/data/{entitySetId}/count"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        integer$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    LoadEntitySetData = function(entity.set.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      urlPath <- "/datastore/data/set/{entitySetId}"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    LoadFilteredEntitySetData = function(entity.set.id, entity.set.selection, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      if (missing(`entity.set.selection`)) {
        stop("Missing required parameter `entity.set.selection`.")
      }

      if (!missing(`entity.set.selection`)) {
        body <- `entity.set.selection`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/data/set/{entitySetId}"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    }
  )
)
