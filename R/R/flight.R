# OpenLattice API
#
# OpenLattice API
#
# The version of the OpenAPI document: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Flight
#' @description Flight Class
#' @format An \code{R6Class} generator object
#' @field name  character [optional]
#'
#' @field tags  list( character ) [optional]
#'
#' @field organizationId  character [optional]
#'
#' @field entityDefinitions  named list( \link{EntityDefinition} ) [optional]
#'
#' @field associationDefinitions  named list( \link{AssociationDefinition} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Flight <- R6::R6Class(
  'Flight',
  public = list(
    `name` = NULL,
    `tags` = NULL,
    `organizationId` = NULL,
    `entityDefinitions` = NULL,
    `associationDefinitions` = NULL,
    initialize = function(`name`=NULL, `tags`=NULL, `organizationId`=NULL, `entityDefinitions`=NULL, `associationDefinitions`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
      if (!is.null(`tags`)) {
        stopifnot(is.vector(`tags`), length(`tags`) != 0)
        sapply(`tags`, function(x) stopifnot(is.character(x)))
        self$`tags` <- `tags`
      }
      if (!is.null(`organizationId`)) {
        stopifnot(is.character(`organizationId`), length(`organizationId`) == 1)
        self$`organizationId` <- `organizationId`
      }
      if (!is.null(`entityDefinitions`)) {
        stopifnot(is.vector(`entityDefinitions`), length(`entityDefinitions`) != 0)
        sapply(`entityDefinitions`, function(x) stopifnot(R6::is.R6(x)))
        self$`entityDefinitions` <- `entityDefinitions`
      }
      if (!is.null(`associationDefinitions`)) {
        stopifnot(is.vector(`associationDefinitions`), length(`associationDefinitions`) != 0)
        sapply(`associationDefinitions`, function(x) stopifnot(R6::is.R6(x)))
        self$`associationDefinitions` <- `associationDefinitions`
      }
    },
    toJSON = function() {
      FlightObject <- list()
      if (!is.null(self$`name`)) {
        FlightObject[['name']] <-
          self$`name`
      }
      if (!is.null(self$`tags`)) {
        FlightObject[['tags']] <-
          self$`tags`
      }
      if (!is.null(self$`organizationId`)) {
        FlightObject[['organizationId']] <-
          self$`organizationId`
      }
      if (!is.null(self$`entityDefinitions`)) {
        FlightObject[['entityDefinitions']] <-
          lapply(self$`entityDefinitions`, function(x) x$toJSON())
      }
      if (!is.null(self$`associationDefinitions`)) {
        FlightObject[['associationDefinitions']] <-
          lapply(self$`associationDefinitions`, function(x) x$toJSON())
      }

      FlightObject
    },
    fromJSON = function(FlightJson) {
      FlightObject <- jsonlite::fromJSON(FlightJson)
      if (!is.null(FlightObject$`name`)) {
        self$`name` <- FlightObject$`name`
      }
      if (!is.null(FlightObject$`tags`)) {
        self$`tags` <- ApiClient$new()$deserializeObj(FlightObject$`tags`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(FlightObject$`organizationId`)) {
        self$`organizationId` <- FlightObject$`organizationId`
      }
      if (!is.null(FlightObject$`entityDefinitions`)) {
        self$`entityDefinitions` <- ApiClient$new()$deserializeObj(FlightObject$`entityDefinitions`, "map(EntityDefinition)", loadNamespace("openlattice"))
      }
      if (!is.null(FlightObject$`associationDefinitions`)) {
        self$`associationDefinitions` <- ApiClient$new()$deserializeObj(FlightObject$`associationDefinitions`, "map(AssociationDefinition)", loadNamespace("openlattice"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`name`)) {
        sprintf(
        '"name":
          "%s"
                ',
        self$`name`
        )},
        if (!is.null(self$`tags`)) {
        sprintf(
        '"tags":
           [%s]
        ',
        paste(unlist(lapply(self$`tags`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`organizationId`)) {
        sprintf(
        '"organizationId":
          "%s"
                ',
        self$`organizationId`
        )},
        if (!is.null(self$`entityDefinitions`)) {
        sprintf(
        '"entityDefinitions":
        %s
',
        jsonlite::toJSON(lapply(self$`entityDefinitions`, function(x){ x$toJSON() }), auto_unbox = TRUE, digits=NA)
        )},
        if (!is.null(self$`associationDefinitions`)) {
        sprintf(
        '"associationDefinitions":
        %s
',
        jsonlite::toJSON(lapply(self$`associationDefinitions`, function(x){ x$toJSON() }), auto_unbox = TRUE, digits=NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(FlightJson) {
      FlightObject <- jsonlite::fromJSON(FlightJson)
      self$`name` <- FlightObject$`name`
      self$`tags` <- ApiClient$new()$deserializeObj(FlightObject$`tags`, "array[character]", loadNamespace("openlattice"))
      self$`organizationId` <- FlightObject$`organizationId`
      self$`entityDefinitions` <- ApiClient$new()$deserializeObj(FlightObject$`entityDefinitions`, "map(EntityDefinition)", loadNamespace("openlattice"))
      self$`associationDefinitions` <- ApiClient$new()$deserializeObj(FlightObject$`associationDefinitions`, "map(AssociationDefinition)", loadNamespace("openlattice"))
      self
    }
  )
)
