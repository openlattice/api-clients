# OpenLattice API
#
# OpenLattice API
#
# OpenAPI spec version: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @title Collections operations
#' @description openlattice.Collections
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' add_type_to_entity_type_collection_template Appends type to template of the specified EntityTypeCollection
#'
#'
#' create_entity_set_collection Creates a new EntitySetCollection
#'
#'
#' create_entity_type_collection Creates a new EntityTypeCollection
#'
#'
#' delete_entity_set_collection Deletes the specified EntitySetCollection
#'
#'
#' delete_entity_type_collection Deletes the specified EntityTypeCollection
#'
#'
#' get_all_entity_set_collections Returns all EntitySetCollection objects
#'
#'
#' get_all_entity_type_collections Returns all EntityTypeCollection objects
#'
#'
#' get_entity_set_collection Returns the EntitySetCollection object for a given id
#'
#'
#' get_entity_set_collections_of_type Returns all authorized EntitySetCollections for a given EntityTypeCollection id
#'
#'
#' get_entity_type_collection Returns the EntityTypeCollection object for a given id
#'
#'
#' remove_type_from_entity_type_collection_template Removes a key from an EntityTypeCollection template
#'
#'
#' update_entity_set_collection_metadata Updates metadata of the specified EntitySetCollection
#'
#'
#' update_entity_set_collection_template Updates template of the specified EntitySetCollection
#'
#'
#' update_entity_type_collection_metadata Updates metadata of the specified EntityTypeCollection
#'
#' }
#'
#' @export
CollectionsApi <- R6::R6Class(
  'CollectionsApi',
  public = list(
    userAgent = "OpenAPI-Generator/0.1.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    add_type_to_entity_type_collection_template = function(entity_type_collection_id, entity_type_collection, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`entity_type_collection`)) {
        body <- `entity_type_collection`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/collections/entity/type/{entityTypeCollectionId}/template"
      if (!missing(`entity_type_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeCollectionId", "\\}"), `entity_type_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
            # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    create_entity_set_collection = function(entity_set_collection, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`entity_set_collection`)) {
        body <- `entity_set_collection`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/collections/entity/set"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    create_entity_type_collection = function(entity_type_collection, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`entity_type_collection`)) {
        body <- `entity_type_collection`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/collections/entity/type"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_entity_set_collection = function(entity_set_collection_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/set/{entitySetCollectionId}"
      if (!missing(`entity_set_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetCollectionId", "\\}"), `entity_set_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
            # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_entity_type_collection = function(entity_type_collection_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/type/{entityTypeCollectionId}"
      if (!missing(`entity_type_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeCollectionId", "\\}"), `entity_type_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
            # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_all_entity_set_collections = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/set"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_all_entity_type_collections = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/type"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_entity_set_collection = function(entity_set_collection_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/set/{entitySetCollectionId}"
      if (!missing(`entity_set_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetCollectionId", "\\}"), `entity_set_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_entity_set_collections_of_type = function(entity_set_collection_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/type/entity/set/{entitySetCollectionId}"
      if (!missing(`entity_set_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetCollectionId", "\\}"), `entity_set_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_entity_type_collection = function(entity_type_collection_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/type/{entityTypeCollectionId}"
      if (!missing(`entity_type_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeCollectionId", "\\}"), `entity_type_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    remove_type_from_entity_type_collection_template = function(entity_type_collection_id, type_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/datastore/collections/entity/type/{entityTypeCollectionId}/template/{typeId}"
      if (!missing(`entity_type_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeCollectionId", "\\}"), `entity_type_collection_id`, urlPath)
      }

      if (!missing(`type_id`)) {
        urlPath <- gsub(paste0("\\{", "typeId", "\\}"), `type_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
            # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_entity_set_collection_metadata = function(entity_set_collection_id, metadata_update, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`metadata_update`)) {
        body <- `metadata_update`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/collections/entity/set/{entitySetCollectionId}"
      if (!missing(`entity_set_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetCollectionId", "\\}"), `entity_set_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
            # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_entity_set_collection_template = function(entity_set_collection_id, request_body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`request_body`)) {
        body <- `request_body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/collections/entity/set/{entitySetCollectionId}/template"
      if (!missing(`entity_set_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetCollectionId", "\\}"), `entity_set_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
            # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_entity_type_collection_metadata = function(entity_type_collection_id, metadata_update, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`metadata_update`)) {
        body <- `metadata_update`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/collections/entity/type/{entityTypeCollectionId}"
      if (!missing(`entity_type_collection_id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeCollectionId", "\\}"), `entity_type_collection_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
            # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
