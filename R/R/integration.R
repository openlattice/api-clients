# OpenLattice API
#
# OpenLattice API
#
# The version of the OpenAPI document: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Integration
#'
#' @description Integration Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field key  character [optional]
#'
#' @field environment  character [optional]
#'
#' @field s3Bucket  character [optional]
#'
#' @field contacts  list( character ) [optional]
#'
#' @field organizationId  character [optional]
#'
#' @field entitySetId  character [optional]
#'
#' @field maxConnections  integer [optional]
#'
#' @field callback  list( character ) [optional]
#'
#' @field flightPlanParameters  named list( \link{FlightPlanParameters} ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Integration <- R6::R6Class(
  'Integration',
  public = list(
    `key` = NULL,
    `environment` = NULL,
    `s3Bucket` = NULL,
    `contacts` = NULL,
    `organizationId` = NULL,
    `entitySetId` = NULL,
    `maxConnections` = NULL,
    `callback` = NULL,
    `flightPlanParameters` = NULL,
    initialize = function(
        `key`=NULL, `environment`=NULL, `s3Bucket`=NULL, `contacts`=NULL, `organizationId`=NULL, `entitySetId`=NULL, `maxConnections`=NULL, `callback`=NULL, `flightPlanParameters`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`key`)) {
        stopifnot(is.character(`key`), length(`key`) == 1)
        self$`key` <- `key`
      }
      if (!is.null(`environment`)) {
        stopifnot(is.character(`environment`), length(`environment`) == 1)
        self$`environment` <- `environment`
      }
      if (!is.null(`s3Bucket`)) {
        stopifnot(is.character(`s3Bucket`), length(`s3Bucket`) == 1)
        self$`s3Bucket` <- `s3Bucket`
      }
      if (!is.null(`contacts`)) {
        stopifnot(is.vector(`contacts`))
        sapply(`contacts`, function(x) stopifnot(is.character(x)))
        self$`contacts` <- `contacts`
      }
      if (!is.null(`organizationId`)) {
        stopifnot(is.character(`organizationId`), length(`organizationId`) == 1)
        self$`organizationId` <- `organizationId`
      }
      if (!is.null(`entitySetId`)) {
        stopifnot(is.character(`entitySetId`), length(`entitySetId`) == 1)
        self$`entitySetId` <- `entitySetId`
      }
      if (!is.null(`maxConnections`)) {
        stopifnot(is.numeric(`maxConnections`), length(`maxConnections`) == 1)
        self$`maxConnections` <- `maxConnections`
      }
      if (!is.null(`callback`)) {
        stopifnot(is.vector(`callback`))
        sapply(`callback`, function(x) stopifnot(is.character(x)))
        self$`callback` <- `callback`
      }
      if (!is.null(`flightPlanParameters`)) {
        stopifnot(is.vector(`flightPlanParameters`))
        sapply(`flightPlanParameters`, function(x) stopifnot(R6::is.R6(x)))
        self$`flightPlanParameters` <- `flightPlanParameters`
      }
    },
    toJSON = function() {
      IntegrationObject <- list()
      if (!is.null(self$`key`)) {
        IntegrationObject[['key']] <-
          self$`key`
      }
      if (!is.null(self$`environment`)) {
        IntegrationObject[['environment']] <-
          self$`environment`
      }
      if (!is.null(self$`s3Bucket`)) {
        IntegrationObject[['s3Bucket']] <-
          self$`s3Bucket`
      }
      if (!is.null(self$`contacts`)) {
        IntegrationObject[['contacts']] <-
          self$`contacts`
      }
      if (!is.null(self$`organizationId`)) {
        IntegrationObject[['organizationId']] <-
          self$`organizationId`
      }
      if (!is.null(self$`entitySetId`)) {
        IntegrationObject[['entitySetId']] <-
          self$`entitySetId`
      }
      if (!is.null(self$`maxConnections`)) {
        IntegrationObject[['maxConnections']] <-
          self$`maxConnections`
      }
      if (!is.null(self$`callback`)) {
        IntegrationObject[['callback']] <-
          self$`callback`
      }
      if (!is.null(self$`flightPlanParameters`)) {
        IntegrationObject[['flightPlanParameters']] <-
          lapply(self$`flightPlanParameters`, function(x) x$toJSON())
      }

      IntegrationObject
    },
    fromJSON = function(IntegrationJson) {
      IntegrationObject <- jsonlite::fromJSON(IntegrationJson)
      if (!is.null(IntegrationObject$`key`)) {
        self$`key` <- IntegrationObject$`key`
      }
      if (!is.null(IntegrationObject$`environment`)) {
        self$`environment` <- IntegrationObject$`environment`
      }
      if (!is.null(IntegrationObject$`s3Bucket`)) {
        self$`s3Bucket` <- IntegrationObject$`s3Bucket`
      }
      if (!is.null(IntegrationObject$`contacts`)) {
        self$`contacts` <- ApiClient$new()$deserializeObj(IntegrationObject$`contacts`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(IntegrationObject$`organizationId`)) {
        self$`organizationId` <- IntegrationObject$`organizationId`
      }
      if (!is.null(IntegrationObject$`entitySetId`)) {
        self$`entitySetId` <- IntegrationObject$`entitySetId`
      }
      if (!is.null(IntegrationObject$`maxConnections`)) {
        self$`maxConnections` <- IntegrationObject$`maxConnections`
      }
      if (!is.null(IntegrationObject$`callback`)) {
        self$`callback` <- ApiClient$new()$deserializeObj(IntegrationObject$`callback`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(IntegrationObject$`flightPlanParameters`)) {
        self$`flightPlanParameters` <- ApiClient$new()$deserializeObj(IntegrationObject$`flightPlanParameters`, "list(FlightPlanParameters)", loadNamespace("openlattice"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`key`)) {
        sprintf(
        '"key":
          "%s"
                ',
        self$`key`
        )},
        if (!is.null(self$`environment`)) {
        sprintf(
        '"environment":
          "%s"
                ',
        self$`environment`
        )},
        if (!is.null(self$`s3Bucket`)) {
        sprintf(
        '"s3Bucket":
          "%s"
                ',
        self$`s3Bucket`
        )},
        if (!is.null(self$`contacts`)) {
        sprintf(
        '"contacts":
           [%s]
        ',
        paste(unlist(lapply(self$`contacts`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`organizationId`)) {
        sprintf(
        '"organizationId":
          "%s"
                ',
        self$`organizationId`
        )},
        if (!is.null(self$`entitySetId`)) {
        sprintf(
        '"entitySetId":
          "%s"
                ',
        self$`entitySetId`
        )},
        if (!is.null(self$`maxConnections`)) {
        sprintf(
        '"maxConnections":
          %d
                ',
        self$`maxConnections`
        )},
        if (!is.null(self$`callback`)) {
        sprintf(
        '"callback":
           [%s]
        ',
        paste(unlist(lapply(self$`callback`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`flightPlanParameters`)) {
        sprintf(
        '"flightPlanParameters":
        %s
',
        jsonlite::toJSON(lapply(self$`flightPlanParameters`, function(x){ x$toJSON() }), auto_unbox = TRUE, digits=NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(IntegrationJson) {
      IntegrationObject <- jsonlite::fromJSON(IntegrationJson)
      self$`key` <- IntegrationObject$`key`
      self$`environment` <- IntegrationObject$`environment`
      self$`s3Bucket` <- IntegrationObject$`s3Bucket`
      self$`contacts` <- ApiClient$new()$deserializeObj(IntegrationObject$`contacts`, "array[character]", loadNamespace("openlattice"))
      self$`organizationId` <- IntegrationObject$`organizationId`
      self$`entitySetId` <- IntegrationObject$`entitySetId`
      self$`maxConnections` <- IntegrationObject$`maxConnections`
      self$`callback` <- ApiClient$new()$deserializeObj(IntegrationObject$`callback`, "array[character]", loadNamespace("openlattice"))
      self$`flightPlanParameters` <- ApiClient$new()$deserializeObj(IntegrationObject$`flightPlanParameters`, "list(FlightPlanParameters)", loadNamespace("openlattice"))
      self
    }
  )
)

