# OpenLattice API
#
# OpenLattice API
#
# The version of the OpenAPI document: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title EDM
#' @description EDM Class
#' @format An \code{R6Class} generator object
#' @field namespaces  list( character ) [optional]
#'
#' @field schemas  list( \link{Schema} ) [optional]
#'
#' @field propertyTypes  list( \link{PropertyType} ) [optional]
#'
#' @field entityTypes  list( \link{EntityType} ) [optional]
#'
#' @field associationTypes  list( \link{AssociationType} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
EDM <- R6::R6Class(
  'EDM',
  public = list(
    `namespaces` = NULL,
    `schemas` = NULL,
    `propertyTypes` = NULL,
    `entityTypes` = NULL,
    `associationTypes` = NULL,
    initialize = function(`namespaces`=NULL, `schemas`=NULL, `propertyTypes`=NULL, `entityTypes`=NULL, `associationTypes`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`namespaces`)) {
        stopifnot(is.vector(`namespaces`), length(`namespaces`) != 0)
        sapply(`namespaces`, function(x) stopifnot(is.character(x)))
        self$`namespaces` <- `namespaces`
      }
      if (!is.null(`schemas`)) {
        stopifnot(is.vector(`schemas`), length(`schemas`) != 0)
        sapply(`schemas`, function(x) stopifnot(R6::is.R6(x)))
        self$`schemas` <- `schemas`
      }
      if (!is.null(`propertyTypes`)) {
        stopifnot(is.vector(`propertyTypes`), length(`propertyTypes`) != 0)
        sapply(`propertyTypes`, function(x) stopifnot(R6::is.R6(x)))
        self$`propertyTypes` <- `propertyTypes`
      }
      if (!is.null(`entityTypes`)) {
        stopifnot(is.vector(`entityTypes`), length(`entityTypes`) != 0)
        sapply(`entityTypes`, function(x) stopifnot(R6::is.R6(x)))
        self$`entityTypes` <- `entityTypes`
      }
      if (!is.null(`associationTypes`)) {
        stopifnot(is.vector(`associationTypes`), length(`associationTypes`) != 0)
        sapply(`associationTypes`, function(x) stopifnot(R6::is.R6(x)))
        self$`associationTypes` <- `associationTypes`
      }
    },
    toJSON = function() {
      EDMObject <- list()
      if (!is.null(self$`namespaces`)) {
        EDMObject[['namespaces']] <-
          self$`namespaces`
      }
      if (!is.null(self$`schemas`)) {
        EDMObject[['schemas']] <-
          lapply(self$`schemas`, function(x) x$toJSON())
      }
      if (!is.null(self$`propertyTypes`)) {
        EDMObject[['propertyTypes']] <-
          lapply(self$`propertyTypes`, function(x) x$toJSON())
      }
      if (!is.null(self$`entityTypes`)) {
        EDMObject[['entityTypes']] <-
          lapply(self$`entityTypes`, function(x) x$toJSON())
      }
      if (!is.null(self$`associationTypes`)) {
        EDMObject[['associationTypes']] <-
          lapply(self$`associationTypes`, function(x) x$toJSON())
      }

      EDMObject
    },
    fromJSON = function(EDMJson) {
      EDMObject <- jsonlite::fromJSON(EDMJson)
      if (!is.null(EDMObject$`namespaces`)) {
        self$`namespaces` <- ApiClient$new()$deserializeObj(EDMObject$`namespaces`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(EDMObject$`schemas`)) {
        self$`schemas` <- ApiClient$new()$deserializeObj(EDMObject$`schemas`, "array[Schema]", loadNamespace("openlattice"))
      }
      if (!is.null(EDMObject$`propertyTypes`)) {
        self$`propertyTypes` <- ApiClient$new()$deserializeObj(EDMObject$`propertyTypes`, "array[PropertyType]", loadNamespace("openlattice"))
      }
      if (!is.null(EDMObject$`entityTypes`)) {
        self$`entityTypes` <- ApiClient$new()$deserializeObj(EDMObject$`entityTypes`, "array[EntityType]", loadNamespace("openlattice"))
      }
      if (!is.null(EDMObject$`associationTypes`)) {
        self$`associationTypes` <- ApiClient$new()$deserializeObj(EDMObject$`associationTypes`, "array[AssociationType]", loadNamespace("openlattice"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`namespaces`)) {
        sprintf(
        '"namespaces":
           [%s]
        ',
        paste(unlist(lapply(self$`namespaces`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`schemas`)) {
        sprintf(
        '"schemas":
        [%s]
',
        paste(sapply(self$`schemas`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`propertyTypes`)) {
        sprintf(
        '"propertyTypes":
        [%s]
',
        paste(sapply(self$`propertyTypes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`entityTypes`)) {
        sprintf(
        '"entityTypes":
        [%s]
',
        paste(sapply(self$`entityTypes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`associationTypes`)) {
        sprintf(
        '"associationTypes":
        [%s]
',
        paste(sapply(self$`associationTypes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(EDMJson) {
      EDMObject <- jsonlite::fromJSON(EDMJson)
      self$`namespaces` <- ApiClient$new()$deserializeObj(EDMObject$`namespaces`, "array[character]", loadNamespace("openlattice"))
      self$`schemas` <- ApiClient$new()$deserializeObj(EDMObject$`schemas`, "array[Schema]", loadNamespace("openlattice"))
      self$`propertyTypes` <- ApiClient$new()$deserializeObj(EDMObject$`propertyTypes`, "array[PropertyType]", loadNamespace("openlattice"))
      self$`entityTypes` <- ApiClient$new()$deserializeObj(EDMObject$`entityTypes`, "array[EntityType]", loadNamespace("openlattice"))
      self$`associationTypes` <- ApiClient$new()$deserializeObj(EDMObject$`associationTypes`, "array[AssociationType]", loadNamespace("openlattice"))
      self
    }
  )
)
