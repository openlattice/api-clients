# OpenLattice API
#
# OpenLattice API
#
# The version of the OpenAPI document: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title NeighborSearchFilter
#' @description NeighborSearchFilter Class
#' @format An \code{R6Class} generator object
#' @field entityKeyIds  list( character ) [optional]
#'
#' @field src  list( character ) [optional]
#'
#' @field dst  list( character ) [optional]
#'
#' @field edge  list( character ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
NeighborSearchFilter <- R6::R6Class(
  'NeighborSearchFilter',
  public = list(
    `entityKeyIds` = NULL,
    `src` = NULL,
    `dst` = NULL,
    `edge` = NULL,
    initialize = function(`entityKeyIds`=NULL, `src`=NULL, `dst`=NULL, `edge`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`entityKeyIds`)) {
        stopifnot(is.vector(`entityKeyIds`))
        sapply(`entityKeyIds`, function(x) stopifnot(is.character(x)))
        self$`entityKeyIds` <- `entityKeyIds`
      }
      if (!is.null(`src`)) {
        stopifnot(is.vector(`src`))
        sapply(`src`, function(x) stopifnot(is.character(x)))
        self$`src` <- `src`
      }
      if (!is.null(`dst`)) {
        stopifnot(is.vector(`dst`))
        sapply(`dst`, function(x) stopifnot(is.character(x)))
        self$`dst` <- `dst`
      }
      if (!is.null(`edge`)) {
        stopifnot(is.vector(`edge`))
        sapply(`edge`, function(x) stopifnot(is.character(x)))
        self$`edge` <- `edge`
      }
    },
    toJSON = function() {
      NeighborSearchFilterObject <- list()
      if (!is.null(self$`entityKeyIds`)) {
        NeighborSearchFilterObject[['entityKeyIds']] <-
          self$`entityKeyIds`
      }
      if (!is.null(self$`src`)) {
        NeighborSearchFilterObject[['src']] <-
          self$`src`
      }
      if (!is.null(self$`dst`)) {
        NeighborSearchFilterObject[['dst']] <-
          self$`dst`
      }
      if (!is.null(self$`edge`)) {
        NeighborSearchFilterObject[['edge']] <-
          self$`edge`
      }

      NeighborSearchFilterObject
    },
    fromJSON = function(NeighborSearchFilterJson) {
      NeighborSearchFilterObject <- jsonlite::fromJSON(NeighborSearchFilterJson)
      if (!is.null(NeighborSearchFilterObject$`entityKeyIds`)) {
        self$`entityKeyIds` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`entityKeyIds`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(NeighborSearchFilterObject$`src`)) {
        self$`src` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`src`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(NeighborSearchFilterObject$`dst`)) {
        self$`dst` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`dst`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(NeighborSearchFilterObject$`edge`)) {
        self$`edge` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`edge`, "array[character]", loadNamespace("openlattice"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`entityKeyIds`)) {
        sprintf(
        '"entityKeyIds":
           [%s]
        ',
        paste(unlist(lapply(self$`entityKeyIds`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`src`)) {
        sprintf(
        '"src":
           [%s]
        ',
        paste(unlist(lapply(self$`src`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`dst`)) {
        sprintf(
        '"dst":
           [%s]
        ',
        paste(unlist(lapply(self$`dst`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`edge`)) {
        sprintf(
        '"edge":
           [%s]
        ',
        paste(unlist(lapply(self$`edge`, function(x) paste0('"', x, '"'))), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(NeighborSearchFilterJson) {
      NeighborSearchFilterObject <- jsonlite::fromJSON(NeighborSearchFilterJson)
      self$`entityKeyIds` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`entityKeyIds`, "array[character]", loadNamespace("openlattice"))
      self$`src` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`src`, "array[character]", loadNamespace("openlattice"))
      self$`dst` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`dst`, "array[character]", loadNamespace("openlattice"))
      self$`edge` <- ApiClient$new()$deserializeObj(NeighborSearchFilterObject$`edge`, "array[character]", loadNamespace("openlattice"))
      self
    }
  )
)
