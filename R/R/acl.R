# OpenLattice API
#
# OpenLattice API
#
# The version of the OpenAPI document: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Acl
#' @description Acl Class
#' @format An \code{R6Class} generator object
#' @field aclKey  list( character ) [optional]
#'
#' @field aces  list( \link{Ace} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Acl <- R6::R6Class(
  'Acl',
  public = list(
    `aclKey` = NULL,
    `aces` = NULL,
    initialize = function(`aclKey`=NULL, `aces`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`aclKey`)) {
        stopifnot(is.vector(`aclKey`))
        sapply(`aclKey`, function(x) stopifnot(is.character(x)))
        self$`aclKey` <- `aclKey`
      }
      if (!is.null(`aces`)) {
        stopifnot(is.vector(`aces`))
        sapply(`aces`, function(x) stopifnot(R6::is.R6(x)))
        self$`aces` <- `aces`
      }
    },
    toJSON = function() {
      AclObject <- list()
      if (!is.null(self$`aclKey`)) {
        AclObject[['aclKey']] <-
          self$`aclKey`
      }
      if (!is.null(self$`aces`)) {
        AclObject[['aces']] <-
          lapply(self$`aces`, function(x) x$toJSON())
      }

      AclObject
    },
    fromJSON = function(AclJson) {
      AclObject <- jsonlite::fromJSON(AclJson)
      if (!is.null(AclObject$`aclKey`)) {
        self$`aclKey` <- ApiClient$new()$deserializeObj(AclObject$`aclKey`, "array[character]", loadNamespace("openlattice"))
      }
      if (!is.null(AclObject$`aces`)) {
        self$`aces` <- ApiClient$new()$deserializeObj(AclObject$`aces`, "array[Ace]", loadNamespace("openlattice"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`aclKey`)) {
        sprintf(
        '"aclKey":
           [%s]
        ',
        paste(unlist(lapply(self$`aclKey`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`aces`)) {
        sprintf(
        '"aces":
        [%s]
',
        paste(sapply(self$`aces`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(AclJson) {
      AclObject <- jsonlite::fromJSON(AclJson)
      self$`aclKey` <- ApiClient$new()$deserializeObj(AclObject$`aclKey`, "array[character]", loadNamespace("openlattice"))
      self$`aces` <- ApiClient$new()$deserializeObj(AclObject$`aces`, "array[Ace]", loadNamespace("openlattice"))
      self
    }
  )
)
