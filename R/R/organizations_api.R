# OpenLattice API
#
# OpenLattice API
#
# OpenAPI spec version: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @title Organizations operations
#' @description openlattice.Organizations
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' AddMember Add member to an organization
#'
#'
#' AddRoleToUser Add a role to a user
#'
#'
#' AssembleEntitySets Materializes entity sets into the organization database.
#'
#'
#' CreateOrganizationIfNotExists Create an organisation if it doesn&#39;t exist.
#'
#'
#' CreateRole Create role
#'
#'
#' DeleteRole Remove role for an organization
#'
#'
#' DestroyOrganization Remove an organisation from the organizationId
#'
#'
#' GetAllUsersOfRole Get members of a role for an organization from a roleId
#'
#'
#' GetAutoApprovedEmailDomains Get auto-approved email domains
#'
#'
#' GetFlaggedOrganizationEntitySets Get the entity sets for an organization for a certain flag
#'
#'
#' GetMembers Get members of a certain organization
#'
#'
#' GetOrganization Get an organisation from the organizationId
#'
#'
#' GetOrganizationEntitySets Get the entity sets for an organization for a certain filter
#'
#'
#' GetOrganizations Get all organisations
#'
#'
#' GetRole Get role for an organization from a roleId
#'
#'
#' GetRoles Get roles for an organization
#'
#'
#' RemoveAutoApprovedEmailDomains Remove auto-approved email domains
#'
#'
#' RemoveMember Remove member from an organization
#'
#'
#' RemoveRoleFromUser Remove a role from a user
#'
#'
#' SetAutoApprovedEmailDomain Set auto-approved email domains
#'
#'
#' UpdateDescription Update the organisation description
#'
#'
#' UpdateRoleDescription Update role description for an organization from a roleId
#'
#'
#' UpdateRoleTitle Update role title for an organization from a roleId
#'
#'
#' UpdateTitle Update the organisation title
#'
#' }
#'
#' @importFrom caTools base64encode
#' @export
OrganizationsApi <- R6::R6Class(
  'OrganizationsApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    AddMember = function(organization.id, user.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`user.id`)) {
        stop("Missing required parameter `user.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/members/{userId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`user.id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), `user.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddRoleToUser = function(organization.id, role.id, user.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`role.id`)) {
        stop("Missing required parameter `role.id`.")
      }

      if (missing(`user.id`)) {
        stop("Missing required parameter `user.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles/{roleId}/members/{userId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`role.id`)) {
        urlPath <- gsub(paste0("\\{", "roleId", "\\}"), `role.id`, urlPath)
      }

      if (!missing(`user.id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), `user.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AssembleEntitySets = function(organization.id, request.body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`request.body`)) {
        stop("Missing required parameter `request.body`.")
      }

      if (!missing(`request.body`)) {
        body <- `request.body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/entitySets/assemble"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateOrganizationIfNotExists = function(organization, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization`)) {
        stop("Missing required parameter `organization`.")
      }

      if (!missing(`organization`)) {
        body <- `organization`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateRole = function(role, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`role`)) {
        stop("Missing required parameter `role`.")
      }

      if (!missing(`role`)) {
        body <- `role`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/roles"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteRole = function(organization.id, role.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`role.id`)) {
        stop("Missing required parameter `role.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles/{roleId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`role.id`)) {
        urlPath <- gsub(paste0("\\{", "roleId", "\\}"), `role.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DestroyOrganization = function(organization.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllUsersOfRole = function(organization.id, role.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`role.id`)) {
        stop("Missing required parameter `role.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles/{roleId}/members/"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`role.id`)) {
        urlPath <- gsub(paste0("\\{", "roleId", "\\}"), `role.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Auth0userBasic$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAutoApprovedEmailDomains = function(organization.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/email-domains"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetFlaggedOrganizationEntitySets = function(organization.id, request.body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`request.body`)) {
        stop("Missing required parameter `request.body`.")
      }

      if (!missing(`request.body`)) {
        body <- `request.body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/entitySets"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetMembers = function(organization.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/members"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        OrganizationMember$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetOrganization = function(organization.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Organization$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetOrganizationEntitySets = function(organization.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/entitySets"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetOrganizations = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/organizations"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Organization$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetRole = function(organization.id, role.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`role.id`)) {
        stop("Missing required parameter `role.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles/{roleId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`role.id`)) {
        urlPath <- gsub(paste0("\\{", "roleId", "\\}"), `role.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Role$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetRoles = function(organization.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Role$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RemoveAutoApprovedEmailDomains = function(organization.id, request.body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`request.body`)) {
        stop("Missing required parameter `request.body`.")
      }

      if (!missing(`request.body`)) {
        body <- `request.body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/email-domains"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RemoveMember = function(organization.id, user.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`user.id`)) {
        stop("Missing required parameter `user.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/members/{userId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`user.id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), `user.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RemoveRoleFromUser = function(organization.id, role.id, user.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`role.id`)) {
        stop("Missing required parameter `role.id`.")
      }

      if (missing(`user.id`)) {
        stop("Missing required parameter `user.id`.")
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles/{roleId}/members/{userId}"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`role.id`)) {
        urlPath <- gsub(paste0("\\{", "roleId", "\\}"), `role.id`, urlPath)
      }

      if (!missing(`user.id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), `user.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    SetAutoApprovedEmailDomain = function(organization.id, request.body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`request.body`)) {
        stop("Missing required parameter `request.body`.")
      }

      if (!missing(`request.body`)) {
        body <- `request.body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/email-domains"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateDescription = function(organization.id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/description"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateRoleDescription = function(organization.id, role.id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`role.id`)) {
        stop("Missing required parameter `role.id`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles/{roleId}/description"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`role.id`)) {
        urlPath <- gsub(paste0("\\{", "roleId", "\\}"), `role.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateRoleTitle = function(organization.id, role.id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`role.id`)) {
        stop("Missing required parameter `role.id`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/principals/roles/{roleId}/title"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      if (!missing(`role.id`)) {
        urlPath <- gsub(paste0("\\{", "roleId", "\\}"), `role.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateTitle = function(organization.id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`organization.id`)) {
        stop("Missing required parameter `organization.id`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/organizations/{organizationId}/title"
      if (!missing(`organization.id`)) {
        urlPath <- gsub(paste0("\\{", "organizationId", "\\}"), `organization.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    }
  )
)
