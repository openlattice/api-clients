# OpenLattice API
#
# OpenLattice API
#
# OpenAPI spec version: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @title DataIntegrations operations
#' @description openlattice.DataIntegrations
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' get_entity_key_ids Get entity key IDs
#'
#'
#' integrate_entity_and_association_data Integrate entity and association data
#'
#' }
#'
#' @export
DataIntegrationsApi <- R6::R6Class(
  'DataIntegrationsApi',
  public = list(
    userAgent = "OpenAPI-Generator/0.1.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    get_entity_key_ids = function(entity_key, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`entity_key`)) {
        body <- `entity_key`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/integration/entityKeyIds"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    integrate_entity_and_association_data = function(detailed_results, bulk_data_creation, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`detailed_results`)) {
        queryParams['detailedResults'] <- detailed_results
      }

      if (!missing(`bulk_data_creation`)) {
        body <- `bulk_data_creation`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/integration"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
                jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
