# OpenLattice API
#
# OpenLattice API
#
# OpenAPI spec version: 0.0.1
# Contact: support@openlattice.com
# Generated by: https://openapi-generator.tech

#' @title Edm operations
#' @description openlattice.Edm
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' AddDstEntityTypeToAssociationType Update the AssociationType dst entity types for the given AssociationType UUID by adding the given EntityType UUID.
#'
#'
#' AddPropertyTypeToEntityType Updates the EntityType definition for the given EntityType UUID by adding the given PropertyType UUID.
#'
#'
#' AddSrcEntityTypeToAssociationType Update the AssociationType src entity types for the given AssociationType UUID by adding the given EntityType UUID.
#'
#'
#' CreateAssociationType Creates a new AssociationType definition, if it doesn\&quot;t exist.
#'
#'
#' CreateEmptySchema Creates an empty schema, if it doesn&#39;t exist. If schema exists then no action is taken.
#'
#'
#' CreateEntitySets Create new EntitySet definitions if they don\&quot;t exist.
#'
#'
#' CreateEntityType Creates a new EntityType definition, if it doesn&#39;t exist.
#'
#'
#' CreatePropertyType Creates a new PropertyType definition, if it doesn\&quot;t exist.
#'
#'
#' CreateSchemaIfNotExists Creates an empty schema, if it doesn&#39;t exist. If schema exists then no action is taken.
#'
#'
#' DeleteAssociationType Delete the AssociationType definition for the given AssociationType UUID.
#'
#'
#' DeleteEntitySet Deletes the EntitySet definition for the given EntitySet UUID.
#'
#'
#' DeleteEntityType Deletes the EntityType definition for the given EntityType UUID.
#'
#'
#' DeletePropertyType Deletes the PropertyType definition for the given PropertyType UUID.
#'
#'
#' ForceDeletePropertyType Deletes the PropertyType definition for the given PropertyType UUID regardless of whether or not there is data associated with it.
#'
#'
#' ForceRemovePropertyTypeFromEntityType Updates the EntityType definition for the given EntityType UUID by removing the given PropertyType UUID, regardless of whether or not there is data associated with the entity type.
#'
#'
#' GetAllAssociationEntityTypes Gets all association EntityType definitions.
#'
#'
#' GetAllAvailableAssociationTypes Get all available associations for the given AssociationType UUID.
#'
#'
#' GetAllEntitySetPropertyMetadata Get all property type metadata for an entity set.
#'
#'
#' GetAllEntitySets Get all EntitySet definitions.
#'
#'
#' GetAllEntityTypes Gets all EntityType definitions.
#'
#'
#' GetAllPropertyTypes Gets all PropertyType definitions.
#'
#'
#' GetAllPropertyTypesInNamespace Gets all PropertyType definitions under the given namespace.
#'
#'
#' GetAllPropertyUsageSummaries Get Property Usage Summary for all properties.
#'
#'
#' GetAssociationType Get the AssociationType definition for the given AssociationType UUID.
#'
#'
#' GetAssociationTypeDetails Get details about the AssociationType for the given AssociationType UUID.
#'
#'
#' GetEntityDataModel Gets the entity data model, including namespaces, schemas, entity types, association types, and property types.
#'
#'
#' GetEntityDataModelDiff Gets the changes between the existing entity data model and the entity data model passed in, including schemas, association types, entity types, and property types.
#'
#'
#' GetEntityDataModelVersion Returns the current entity data model version.
#'
#'
#' GetEntitySet Get the EntitySet definition for the given EntitySet UUID.
#'
#'
#' GetEntitySetId Gets the EntitySet UUID for the given EntitySet name.
#'
#'
#' GetEntitySetPropertyMetadata Get specified property type metadata for an entity set.
#'
#'
#' GetEntityType Gets the EntityType definition for the given EntityType UUID.
#'
#'
#' GetEntityTypeHierarchy Get the EntityType hierarchy for the given EntityType UUID.
#'
#'
#' GetEntityTypeId Gets the EntityType UUID for the given EntityType FQN.
#'
#'
#' GetPropertyType Gets the PropertyType definition for the given PropertyType UUID.
#'
#'
#' GetPropertyTypeId Gets the PropertyType UUID for the given PropertyType FQN.
#'
#'
#' GetPropertyUsageSummary Get Property Usage Summary for property with given ID.
#'
#'
#' GetSchemaContents Gets the schema contents for a corresponding namespace and name.
#'
#'
#' GetSchemas Gets all schemas.
#'
#'
#' GetSchemasInNamespace Gets all schemas associated with a given namespace and accessible by the caller.
#'
#'
#' RemoveDstEntityTypeFromAssociationType Updates the AssociationType dst entity types for the given AssociationType UUID by removing the given EntityType UUID.
#'
#'
#' RemovePropertyTypeFromEntityType Updates the EntityType definition for the given EntityType UUID by removing the given PropertyType UUID.
#'
#'
#' RemoveSrcEntityTypeFromAssociationType Updates the AssociationType src entity types for the given AssociationType UUID by removing the given EntityType UUID.
#'
#'
#' ReorderPropertyTypesInEntityType Updates the EntityType definition for the given EntityType UUID by reordering its properties as specified by the provided list.
#'
#'
#' UpdateEntityDataModel Updates the entity data model, including schemas, entity types, association types, and property types.
#'
#'
#' UpdateEntitySetMetaData Updates the EntityType definition for the given EntitySet UUID with the given metadata.
#'
#'
#' UpdateEntitySetPropertyMetadata Updates the property type metadata for the given entity set.
#'
#'
#' UpdateEntityTypeMetaData Updates the EntityType definition for the given EntityType UUID with the given metadata.
#'
#'
#' UpdatePropertyTypeMetaData Updates the PropertyType definition for the given PropertyType UUID with the given metadata.
#'
#'
#' UpdateSchema Edits the schema contents for a corresponding namespace and name.
#'
#' }
#'
#' @importFrom caTools base64encode
#' @export
EdmApi <- R6::R6Class(
  'EdmApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    AddDstEntityTypeToAssociationType = function(association.type.id, entity.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}/dst/{entityTypeId}"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }

      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddPropertyTypeToEntityType = function(entity.type.id, property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}/{propertyTypeId}"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddSrcEntityTypeToAssociationType = function(association.type.id, entity.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}/src/{entityTypeId}"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }

      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateAssociationType = function(association.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type`)) {
        stop("Missing required parameter `association.type`.")
      }

      if (!missing(`association.type`)) {
        body <- `association.type`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/association/type/"

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateEmptySchema = function(namespace, name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`namespace`)) {
        stop("Missing required parameter `namespace`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      urlPath <- "/datastore/edm/schema/{namespace}/{name}"
      if (!missing(`namespace`)) {
        urlPath <- gsub(paste0("\\{", "namespace", "\\}"), `namespace`, urlPath)
      }

      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateEntitySets = function(entity.set, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set`)) {
        stop("Missing required parameter `entity.set`.")
      }

      if (!missing(`entity.set`)) {
        body <- `entity.set`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/entity/set"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateEntityType = function(entity.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type`)) {
        stop("Missing required parameter `entity.type`.")
      }

      if (!missing(`entity.type`)) {
        body <- `entity.type`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/entity/type/"

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreatePropertyType = function(property.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`property.type`)) {
        stop("Missing required parameter `property.type`.")
      }

      if (!missing(`property.type`)) {
        body <- `property.type`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/property/type/"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateSchemaIfNotExists = function(schema, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`schema`)) {
        stop("Missing required parameter `schema`.")
      }

      if (!missing(`schema`)) {
        body <- `schema`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/schema"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteAssociationType = function(association.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }


      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteEntitySet = function(entity.set.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      urlPath <- "/datastore/edm/entity/set/{entitySetId}"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteEntityType = function(entity.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }


      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeletePropertyType = function(property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/property/type/{propertyTypeId}"
      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ForceDeletePropertyType = function(property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/property/type/{propertyTypeId}/force"
      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ForceRemovePropertyTypeFromEntityType = function(entity.type.id, property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}/{propertyTypeId}/force"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllAssociationEntityTypes = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/association/type/"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AssociationType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllAvailableAssociationTypes = function(association.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}/available"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AssociationType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllEntitySetPropertyMetadata = function(entity.set.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      urlPath <- "/datastore/edm/entity/set/{entitySetId}/property/type"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        PropertyType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllEntitySets = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/entity/set"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EntitySet$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllEntityTypes = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/entity/type/"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EntityType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllPropertyTypes = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/property/type/"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        PropertyType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllPropertyTypesInNamespace = function(namespace, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`namespace`)) {
        stop("Missing required parameter `namespace`.")
      }

      urlPath <- "/datastore/edm/property/type/namespace/{namespace}"
      if (!missing(`namespace`)) {
        urlPath <- gsub(paste0("\\{", "namespace", "\\}"), `namespace`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        PropertyType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllPropertyUsageSummaries = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/summary"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        PropertyUsageSummary$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAssociationType = function(association.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AssociationType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAssociationTypeDetails = function(association.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}/detailed"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AssociationType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntityDataModel = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/"

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EDM$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntityDataModelDiff = function(ed.mdiff, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`ed.mdiff`)) {
        stop("Missing required parameter `ed.mdiff`.")
      }

      if (!missing(`ed.mdiff`)) {
        body <- `ed.mdiff`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/diff/"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EDMdiff$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntityDataModelVersion = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/version/"

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntitySet = function(entity.set.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      urlPath <- "/datastore/edm/entity/set/{entitySetId}"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EntitySet$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntitySetId = function(entity.set.name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.name`)) {
        stop("Missing required parameter `entity.set.name`.")
      }

      urlPath <- "/datastore/edm/ids/entity/set/{entitySetName}"
      if (!missing(`entity.set.name`)) {
        urlPath <- gsub(paste0("\\{", "entitySetName", "\\}"), `entity.set.name`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntitySetPropertyMetadata = function(entity.set.id, property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/entity/set/{entitySetId}/property/type/{propertyTypeId}/"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        PropertyType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntityType = function(entity.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }


      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EntityType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntityTypeHierarchy = function(entity.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}/hierarchy"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEntityTypeId = function(namespace, name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`namespace`)) {
        stop("Missing required parameter `namespace`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      urlPath <- "/datastore/edm/ids/entity/type/{namespace}/{name}"
      if (!missing(`namespace`)) {
        urlPath <- gsub(paste0("\\{", "namespace", "\\}"), `namespace`, urlPath)
      }

      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetPropertyType = function(property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/property/type/{propertyTypeId}"
      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        PropertyType$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetPropertyTypeId = function(namespace, name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`namespace`)) {
        stop("Missing required parameter `namespace`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      urlPath <- "/datastore/edm/ids/property/type/{namespace}/{name}"
      if (!missing(`namespace`)) {
        urlPath <- gsub(paste0("\\{", "namespace", "\\}"), `namespace`, urlPath)
      }

      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetPropertyUsageSummary = function(property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/summary/{propertyTypeId}"
      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        PropertyUsageSummary$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetSchemaContents = function(namespace, name, file.type='yaml', ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`namespace`)) {
        stop("Missing required parameter `namespace`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      queryParams['fileType'] <- file.type

      urlPath <- "/datastore/edm/schema/{namespace}/{name}"
      if (!missing(`namespace`)) {
        urlPath <- gsub(paste0("\\{", "namespace", "\\}"), `namespace`, urlPath)
      }

      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Schema$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetSchemas = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/datastore/edm/schema"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Schema$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetSchemasInNamespace = function(namespace, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`namespace`)) {
        stop("Missing required parameter `namespace`.")
      }

      urlPath <- "/datastore/edm/schema/{namespace}"
      if (!missing(`namespace`)) {
        urlPath <- gsub(paste0("\\{", "namespace", "\\}"), `namespace`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Schema$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RemoveDstEntityTypeFromAssociationType = function(association.type.id, entity.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}/dst/{entityTypeId}"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }

      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RemovePropertyTypeFromEntityType = function(entity.type.id, property.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}/{propertyTypeId}"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RemoveSrcEntityTypeFromAssociationType = function(association.type.id, entity.type.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`association.type.id`)) {
        stop("Missing required parameter `association.type.id`.")
      }

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      urlPath <- "/datastore/edm/association/type/{associationTypeId}/src/{entityTypeId}"
      if (!missing(`association.type.id`)) {
        urlPath <- gsub(paste0("\\{", "associationTypeId", "\\}"), `association.type.id`, urlPath)
      }

      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ReorderPropertyTypesInEntityType = function(entity.type.id, property.type.ids, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      if (missing(`property.type.ids`)) {
        stop("Missing required parameter `property.type.ids`.")
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}/property/type"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      if (!missing(`property.type.ids`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeIds", "\\}"), `property.type.ids`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateEntityDataModel = function(EDM, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`EDM`)) {
        stop("Missing required parameter `EDM`.")
      }

      if (!missing(`EDM`)) {
        body <- `EDM`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/"
      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateEntitySetMetaData = function(entity.set.id, meta.data.update, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      if (missing(`meta.data.update`)) {
        stop("Missing required parameter `meta.data.update`.")
      }

      if (!missing(`meta.data.update`)) {
        body <- `meta.data.update`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/entity/set/{entitySetId}"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateEntitySetPropertyMetadata = function(entity.set.id, property.type.id, property.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.set.id`)) {
        stop("Missing required parameter `entity.set.id`.")
      }

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      if (missing(`property.type`)) {
        stop("Missing required parameter `property.type`.")
      }

      if (!missing(`property.type`)) {
        body <- `property.type`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/entity/set/{entitySetId}/property/type/{propertyTypeId}/"
      if (!missing(`entity.set.id`)) {
        urlPath <- gsub(paste0("\\{", "entitySetId", "\\}"), `entity.set.id`, urlPath)
      }

      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateEntityTypeMetaData = function(entity.type.id, entity.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`entity.type.id`)) {
        stop("Missing required parameter `entity.type.id`.")
      }

      if (missing(`entity.type`)) {
        stop("Missing required parameter `entity.type`.")
      }

      if (!missing(`entity.type`)) {
        body <- `entity.type`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/entity/type/{entityTypeId}"
      if (!missing(`entity.type.id`)) {
        urlPath <- gsub(paste0("\\{", "entityTypeId", "\\}"), `entity.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdatePropertyTypeMetaData = function(property.type.id, property.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`property.type.id`)) {
        stop("Missing required parameter `property.type.id`.")
      }

      if (missing(`property.type`)) {
        stop("Missing required parameter `property.type`.")
      }

      if (!missing(`property.type`)) {
        body <- `property.type`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/property/type/{propertyTypeId}"
      if (!missing(`property.type.id`)) {
        urlPath <- gsub(paste0("\\{", "propertyTypeId", "\\}"), `property.type.id`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateSchema = function(namespace, name, edm.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`namespace`)) {
        stop("Missing required parameter `namespace`.")
      }

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`edm.request`)) {
        stop("Missing required parameter `edm.request`.")
      }

      if (!missing(`edm.request`)) {
        body <- `edm.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/datastore/edm/schema/{namespace}/{name}"
      if (!missing(`namespace`)) {
        urlPath <- gsub(paste0("\\{", "namespace", "\\}"), `namespace`, urlPath)
      }

      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      # API key authentication
      if ("Authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["Authorization"]) > 0) {
        headerParams['Authorization'] <- paste(unlist(self$apiClient$apiKeys["Authorization"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    }
  )
)
